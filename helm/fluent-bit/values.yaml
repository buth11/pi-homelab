config:
  outputs: |
    [OUTPUT]
        Name            opensearch
        Match           *
        Host            opensearch-cluster-master.logging.svc.cluster.local
        Port            9200
        Index           fluent-bit
        tls             Off
        Suppress_Type_Name On
        Replace_Dots    On
