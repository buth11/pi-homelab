apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncthing-config-smb
  namespace: syncthing
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: smb-tank-bulk-syncthing
