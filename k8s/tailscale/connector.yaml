apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: homelab-connector
spec:
  hostname: pi-homelab-subnet
  tags:
    - tag:k8s-operator
  subnetRouter:
    advertiseRoutes:
      - "192.168.50.0/24"
