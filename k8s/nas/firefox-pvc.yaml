apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefox-config-nas
  namespace: qbittorrent
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: nas-smb
